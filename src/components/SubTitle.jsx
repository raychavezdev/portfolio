const SectionTitle = ({ children }) => {
  return (
    <h2 className="mb-8 mt-24 flex items-center gap-2 text-3xl font-semibold dark:text-blue-300">
      {children}
    </h2>
  )
}

export default SectionTitle
